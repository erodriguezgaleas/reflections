When would you want to use a remote repository rather than keeping all your work
local?

When we have multiple users access and edit the common project.

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?

Tener un punto de control lógico sobre los commits en los archivos

Describe the differences between forks, clones, and branches. When would you use one instead of another?

frosk is used to copy a git repository between github respositories, clone is uses to copy repository in local or between local and remote and brnaces is to create version over a git repositories.